"""
ComfyUI VFX Bridge - Node Definitions
"""

import os
import glob
import hashlib
import torch
import numpy as np

# Node implementations will be added here

# =============================================================================
# NODE MAPPINGS
# =============================================================================

NODE_CLASS_MAPPINGS = {
    # Nodes will be registered here as we build them
}

NODE_DISPLAY_NAME_MAPPINGS = {
    # Display names will be registered here
}
